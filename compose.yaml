services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
      PORT: ${PORT}
      BACKEND_URL: ${BACKEND_URL}
    ports:
      - '${PORT}:${PORT}'
    restart: always
